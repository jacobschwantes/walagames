import { NextPageContext, NextComponentType } from "next";
interface HeaderProps {}
const Header: NextComponentType<NextPageContext, {}, HeaderProps> = async ({}) => {
  return (
    <header className="w-full px-4">
    
    </header>
  );
};
export default Header;
